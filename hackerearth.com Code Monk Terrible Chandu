/*https://www.hackerearth.com/code-monk-array-strings/algorithm/terrible-chandu/description/*/
// psudocoder

#include <stdio.h>
#include<string.h>
int main()
{
    char ch[30],rh[30];
    int t,i;
   	scanf("%d",&t);
   	while(t--)
   	{
   	    int j=0;
   		scanf("%s",&ch);
   		for(i=strlen(ch)-1;i>=0;i--)
   		{
   			printf("%c",ch[i]);
   		}
   		printf("\n");
   	}
}
